su #after logging in as a user, use SU to log in as a superuser(root) -will ask for root PW
findmnt #check where mounted
mount 
lsblk -f #check all drives
pacman -S <package name> #installs a package
useradd -m -G additional_groups -s login_shell username #user add command login shell = '/bin/bash' https://wiki.archlinux.org/title/Users_and_groups#User_management
passwd username #need to add a PW for the new user

Connect to Wifi (ensure to install network manager in chroot after first install): https://wiki.archlinux.org/title/NetworkManager#Usage
  nmcli device wifi list #lists all wifi devices
  nmcli device wifi connect SSID_or_BSSID password password

chmod #change mod(?) 777 = full permisSeans
chown #change owner
rm -r <file> #removes all recursively, doesn't put it in trash, it's gone - forever - so be careful  
rm -f <file> #removes file with force - it will give you a confirmASean for each file to be removed

